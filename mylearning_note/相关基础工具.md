有关pandas的相关内容：

| **场景** | **核心函数**                    | **掌握重点**                    |
| ------ | --------------------------- | --------------------------- |
| **进场** | `read_csv`                  | 别忘了 `names`（列名）和 `sep`（分隔符） |
| **体检** | `info`, `describe`, `shape` | **不要给 shape 加括号！**          |
| **切片** | `iloc[:, :-1]`              | 冒号代表范围，负号代表倒数               |
| **清理** | `dropna`, `fillna`          | 训练前必须保证没有 `NaN`             |
| **变形** | `insert`, `concat`          | 增加截距项 $1$ 或合并表格             |
| **出口** | `.values`                   | **转 Numpy 是转 Tensor 的唯一桥梁** |
[相关代码实现](vscode://file/C:\Users\YYY\Desktop\mashine_learning\jupyter\relative_skills_learing\learning_pandas.ipynb)

有关numpy的相关内容：

| **场景** | **NumPy 函数**       | **对应 PyTorch**  | **作用**     |
| ------ | ------------------ | --------------- | ---------- |
| **创建** | `np.zeros()`       | `torch.zeros()` | 初始化权重      |
| **变形** | `reshape(-1, 1)`   | `view(-1, 1)`   | 确保矩阵维度对齐   |
| **相乘** | `A @ B`            | `A @ B`         | 神经网络的前向传播  |
| **转置** | `A.T`              | `A.T`           | 梯度公式中的转置运算 |
| **拼合** | `np.concatenate()` | `torch.cat()`   | 特征拼接       |
[相关代码实现](vscode://file/C:\Users\YYY\Desktop\mashine_learning\jupyter\relative_skills_learing\learning_numpy.ipynb)

有关matplotlib.pyplot和seaborn的相关学习

| **函数**            | **作用**    | **深度学习场景**         |
| ----------------- | --------- | ------------------ |
| `plt.plot()`      | 连点成线      | 监控 Loss 下降、画回归拟合线  |
| `plt.scatter()`   | 绘制离散点     | 观察原始数据分布、寻找离群点     |
| `plt.xlabel()`    | 设置 X 轴标签  | 标注“迭代次数”或“特征名”     |
| `plt.figure()`    | 调整画布大小    | 导出高清论文配图 (dpi=300) |
| **函数**            | **作用**    | **深度学习阶段**         |
| `sns.set_theme()` | 一键切换精美主题  | 绘图开始前              |
| `sns.lmplot()`    | 散点图 + 拟合线 | 验证线性回归结果           |
| `sns.pairplot()`  | 全变量矩阵对比   | **多变量特征选择**        |
| `sns.heatmap()`   | 相关性热力图    | 检查特征冗余             |
[相关代码实现](vscode://file/C:\Users\YYY\Desktop\mashine_learning\jupyter\relative_skills_learing\learning_matplotlib_and_seaborn.ipynb)
